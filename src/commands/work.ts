import { Command } from "../interfaces";

const command: Command = {
  name: "work",
  aliases: [],
  permissions: [],
  usage: "",
  run: async (client, msg, args) => {
    // Code...
  },
};

export default command;
